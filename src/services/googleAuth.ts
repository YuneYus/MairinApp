// This handles Google login only
/*
import {
    GoogleAuthProvider,
    signInWithCredential
} from "firebase/auth";


// Import the Firebase auth connection
// that we already created

import {
    auth
} from "./firebase";



// Function called when user presses
// "Login with Google"

export async function signInWithGoogle(
  idToken:string
) {


  // Create a Firebase credential
  // using the Google token

  const credential =
    GoogleAuthProvider.credential(
      idToken
    );



  // Login Firebase user

  return signInWithCredential(
    auth,
    credential
  );

}*/